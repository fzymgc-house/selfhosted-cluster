name: Pull Request Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  validate:
    name: Validate Changes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v46
        with:
          files: |
            **/*.yaml
            **/*.yml

      - name: Install yamllint
        if: steps.changed-files.outputs.any_changed == 'true'
        run: pip install yamllint

      - name: Lint YAML files
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          for file in ${{ steps.changed-files.outputs.all_changed_files }}; do
            # Skip Jinja2 templates (contain {{ }} which fails yamllint braces rule)
            if [[ "$file" == *.j2 ]]; then
              echo "Skipping Jinja2 template: $file"
              continue
            fi
            # Skip ansible files (handled by ansible-lint in pre-commit)
            if [[ "$file" == ansible/** ]]; then
              echo "Skipping ansible file: $file"
              continue
            fi
            echo "Linting $file"
            yamllint -c .yamllint.yaml "$file"
          done

  label:
    name: Auto Label PR
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Label PR
        uses: actions/labeler@v5
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}

  size:
    name: PR Size Check
    runs-on: ubuntu-latest
    steps:
      - name: Check PR size
        uses: actions/github-script@v7
        with:
          script: |
            const pr = context.payload.pull_request;
            const additions = pr.additions;
            const deletions = pr.deletions;
            const total = additions + deletions;

            let size = 'small';
            if (total > 1000) size = 'xlarge';
            else if (total > 500) size = 'large';
            else if (total > 100) size = 'medium';

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr.number,
              labels: [`size/${size}`]
            });
