# SPDX-License-Identifier: MIT-0
---
# handlers file for k3sup

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart multipathd
  ansible.builtin.systemd:
    name: multipathd
    state: restarted
  tags:
    - multipath
