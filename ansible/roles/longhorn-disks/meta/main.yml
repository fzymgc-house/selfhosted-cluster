# SPDX-License-Identifier: MIT-0
# code: language=ansible
---
galaxy_info:
  author: fzymgc-house
  description: Configure additional block devices for Longhorn storage
  license: MIT-0
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - bookworm
    - name: Ubuntu
      versions:
        - noble

dependencies: []

# Required collections (install with: ansible-galaxy collection install -r requirements.yml)
# - ansible.posix >= 1.5.0
# - kubernetes.core >= 2.4.0
