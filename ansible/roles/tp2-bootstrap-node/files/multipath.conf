defaults {
  user_friendly_names yes
  find_multipaths yes
}

# Blacklist Longhorn iSCSI devices
blacklist {
    # Exclude devices by vendor/product
    device {
        vendor "IET"
        product "VIRTUAL-DISK"
    }
}
