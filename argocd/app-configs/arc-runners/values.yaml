# GitHub configuration
githubConfigUrl: "https://github.com/fzymgc-house"
githubConfigSecret: github-token

# Controller service account (in arc-systems namespace)
controllerServiceAccount:
  name: arc-controller-gha-rs-controller
  namespace: arc-systems

# Runner scale set name (visible in GitHub UI)
runnerScaleSetName: "fzymgc-house-cluster-runners"

# Autoscaling configuration
minRunners: 1
maxRunners: 3

# Runner group configuration
runnerGroup: "Default"

# Listener metrics (required in 0.13.0 for listener to start)
listenerMetrics: {}

# Runner pod template - simplified without kubernetes containerMode
# Jobs will run directly on the runner pod
template:
  spec:
    containers:
      - name: runner
        image: ghcr.io/actions/actions-runner:latest
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            cpu: "2"
            memory: 4Gi
          requests:
            cpu: "1"
            memory: 2Gi
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
