# GitHub configuration
githubConfigUrl: "https://github.com/fzymgc-house"
githubConfigSecret: github-token

# Controller service account (in arc-systems namespace)
controllerServiceAccount:
  name: arc-controller-gha-rs-controller
  namespace: arc-systems

# Runner scale set name (visible in GitHub UI)
runnerScaleSetName: "fzymgc-house-cluster-runners"

# Autoscaling configuration
minRunners: 0
maxRunners: 3

# Runner group configuration
runnerGroup: "Default"

# Listener metrics (required in 0.13.0 for listener to start)
listenerMetrics: {}

# Docker-in-Docker mode - use built-in configuration
containerMode:
  type: "dind"

# Runner pod template customization
# Uses custom image with internal CA certs and tools (vault, jq, gh) pre-installed
# See: images/actions-runner/README.md
template:
  spec:
    containers:
      - name: runner
        image: ghcr.io/fzymgc-house/actions-runner:2.330.0
        command: ["/home/runner/run.sh"]
        env:
          # Node.js doesn't use system CA store by default - point to system bundle
          # Required for vault-action and other Node.js-based GitHub Actions
          - name: NODE_EXTRA_CA_CERTS
            value: /etc/ssl/certs/ca-certificates.crt
        resources:
          limits:
            cpu: "2"
            memory: 4Gi
          requests:
            cpu: "500m"
            memory: 1Gi
