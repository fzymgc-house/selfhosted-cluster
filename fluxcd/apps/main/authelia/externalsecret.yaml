apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: authelia-secrets
  namespace: authelia
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: authelia-secrets
    template:
      type: Opaque
  data:
    - secretKey: oidc.client.vault.secret
      remoteRef:
        key: secret/data/authelia/oidc/vault
        property: client_secret
    - secretKey: oidc.client.vault.id
      remoteRef:
        key: secret/data/authelia/oidc/vault
        property: client_id
