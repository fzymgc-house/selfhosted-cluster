apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: loki-s3
  namespace: monitoring
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: loki-s3
    creationPolicy: Owner
  data:
    - secretKey: S3_BUCKET
      remoteRef:
        key: fzymgc-house/cluster/loki
        property: bucket
    - secretKey: S3_ENDPOINT
      remoteRef:
        key: fzymgc-house/cluster/loki
        property: endpoint
    - secretKey: S3_REGION
      remoteRef:
        key: fzymgc-house/cluster/loki
        property: region
    - secretKey: S3_ACCESS_KEY
      remoteRef:
        key: fzymgc-house/cluster/loki
        property: r2_access_key_id
    - secretKey: S3_SECRET_KEY
      remoteRef:
        key: fzymgc-house/cluster/loki
        property: r2_secret_access_key
