# Python dependencies for Ansible automation
# Install with: pip install -r requirements.txt

# Ansible core - latest stable
ansible-core>=2.18.0,<3.0.0

# Ansible full package (includes community collections)
ansible>=13.1.0,<13.1.1

# Required for various Ansible modules
jmespath>=1.0.1        # JSON query language for filters
netaddr>=1.3.0         # IP address manipulation
dnspython>=2.7.0       # DNS toolkit
requests>=2.32.0       # HTTP library
PyYAML>=6.0.0          # YAML parser
Jinja2>=3.1.0          # Templating engine

# Kubernetes/k3s management
kubernetes>=31.0.0     # Python Kubernetes client
openshift>=0.13.2      # OpenShift/k8s dynamic client

# Vault integration
hvac>=2.3.0            # HashiCorp Vault client

# SSH and system management
paramiko>=3.5.0        # SSH protocol library
cryptography>=44.0.0   # Cryptographic recipes

# Ansible dev tools
ansible-dev-tools>=25.12.0

# Linting and validation (optional but recommended)
ansible-lint>=24.10.0  # Ansible best practices linter
yamllint>=1.35.0       # YAML linter
