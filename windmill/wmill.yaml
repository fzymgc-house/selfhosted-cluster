workspace: terraform-gitops
version: v2
defaultRuntime: python3
defaultTs: bun

includes:
  - "**"

excludes: []

# Sync configuration - sync everything except workspace encryption key and variables
# Variables are managed by the sync-vault-to-windmill-vars.sh script
skipVariables: false
skipResources: false
skipResourceTypes: false
skipSecrets: false
skipScripts: false
skipFlows: false
skipApps: false
skipFolders: false

includeSchedules: true
includeTriggers: true
includeUsers: true
includeGroups: true
includeSettings: true
includeKey: false  # Don't sync workspace encryption key

# Default to Python for all scripts
codebases:
  - relativePath: "."
    defaultRuntime: python3
    defaultTs: bun
